<% if current_user.following?(user) %>
  <%= link_to "フォロー解除", unfollow_path(user.id), method: :post, remote: true %>
<% else %>
    <%= link_to "フォローする", follow_path(user.id), method: :post, remote: true %>
<% end %>
