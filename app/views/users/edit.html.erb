<div class= "container">
  <% if @user.errors.any? %>
    <div class= "error_explanation">
      <p><%= @user.errors.count %>件の入力の誤りがあります</p>
      <div class= "error_messages">
        <% @user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </div>
    </div>
  <% end %>
  <h1>ユーザー編集</h1>
  <%= form_for(@user) do |f| %>
    <div class= "user_icon_edit">
      <%= attachment_image_tag @user, :user_image, size: "40x40", format: "jpeg", fallback: "default-icon-img.jpeg", class: "rounded-circle" %>
      <label>アイコン画像</label>
      <%= f.attachment_field :user_image %>
    </div>
    <div class= "user_name_edit">
      <label>名前</label>
      <%= f.text_field :name,placeholder: "名前" %>
    </div>
    <div class= "user_introduction_edit">
      <label>紹介文</label>
      <%= f.text_area :introduction, placeholder: "紹介文" %>
    </div>
    <%= f.submit "変更を保存する" %>
  <% end %>
</div>