<div class = "user-index">
  <h5>ユーザー</h5>
  <table class= "table-dark table-striped rounded users_table">
    <% users.each do |user| %>
      <% unless user == current_user %>
        <tr>
          <td height= "50" class= "pl-1 pr-1">
            <%= link_to user_path(user) do %>
              <%= attachment_image_tag user, :user_image, size: "40x40", format: "jpeg", fallback: asset_path("default-icon-img.jpeg"),class: "rounded-circle" %>
            <% end %>
          </td>
          <td align= "left" class= "w-75 pl-2"><%= link_to user.name, user_path(user) %></td>
        </tr>
      <% end %>
    <% end %>
    <%= paginate users, param_name: "users_pagina", class: "paginate", remote: true %>
  </table>
  <p>ユーザーをフォローして繋がりましょう！</p>
</div>