<div class = "container">
  <div class= "row">
    <table>
      <% if user_signed_in? %>
        <% users.each do |user| %>
          <tr>
            <% unless user == current_user %>
              <%= link_to user_path(user) do %>
                <%= attachment_image_tag user, :user_image, size: "40x40", format: "jpeg", fallback: "default-icon-img.jpeg" %>
                <%= user.name %>
              <% end %>
              <%= render "users/follow_link", user: user %>
            <% end %>
          </tr>
        <% end %>
      <% end %>
    </table>
  </div>
</div>