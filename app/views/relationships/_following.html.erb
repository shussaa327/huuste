<div class = "following_index follow_user_index_content">
  <h4>フォロー中</h4>
  <% if user.following_user.where.not(id: user.id).count < 1 %>
    <p>フォロー中のユーザーはいません</p>
  <% else %>
    <div class= "follow_user_index">
      <% user.following_user.where.not(id: user.id).each do |user| %>
        <div class = "following_user follow_people">
          <%= attachment_image_tag user, :user_image, size: "40x40", format: "jpeg", fallback: "default-icon-img.jpeg", class: "rounded-circle" %>
          <%= user.name %>
          <%= link_to "詳細", user_path(user) %>
        </div>
      <% end %>
    </div>
  <% end %>
</div>