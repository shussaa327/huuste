<div class= "post-index">
  <h1>snap shots</h1>
  <div class= "post_items clearfix">
    <% if posts.count > 0 %>
      <% posts.each do |post| %>
        <div class= "post_item">
          <%= link_to post_path(post) do %>
            <%= attachment_image_tag post, :image, size: "300x300", format: "jpeg", class: "mt-2" %>
          <% end %>
          <p><%= post.shop.area %> 店舗：<%= post.shop.name %></p>
        </div>
      <% end %>
    <% else %>
      <p>まずは気になる人をフォローしてみましょう！</p>
    <% end %>
  </div>
</div>