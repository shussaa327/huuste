<div class= "container">
 <%= render "shops/show", shop: @shop %>
 <div class= "shop_show_posts_section">
  <% unless @shop_posts.count < 1 %>
    <h1>snap shots</h1>
    <%= render "posts/index", posts: @shop_posts %>
  <% end %>
 </div>
</div>