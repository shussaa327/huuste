<!DOCTYPE html>
<html>
  <head>
    <title>Huuste</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div id= "wrapper">
      <header class= "sticky-top">
        <nav class="navbar navbar-expand-md">
          <%= link_to "Huuste", root_path, class: "navbar-brand pr-3" %>
          <div class ="form-inline search">
            <%= form_tag(shops_path, :method => "get") do %>
              <%= text_field_tag :search_area,"",placeholder: "地域" %>
              <p>x</p>
              <%= text_field_tag :search_shop,"",placeholder: "お店の名前" %>
              <%= submit_tag "検索" %>
            <% end %>
          </div>
          <div class="collapse navbar-collapse justify-content-end" id="Navber">
            <ul class="navbar-nav">
              <li class="nav-item pr-3"><%= link_to "Huusteとは？", about_path %></li>
              <% if user_signed_in? %>
                <li class= "nav-item pr-3"><%= link_to " My page", user_path(current_user),class: "fas fa-user" %></li>
                <li class="nav-item pr-4"><%= link_to " Log out", destroy_user_session_path, method: :delete,class: "fas fa-sign-out-alt" %></li>
              <% else %>
                <li class="nav-item pr-3"><%= link_to " Sign up", new_user_registration_path,class: "fas fa-utensils" %></li>
                <li class="nav-item pr-4"><%= link_to " Log in", new_user_session_path,class: "fas fa-sign-in-alt" %></li>
              <% end %>
            </ul>
          </div>
        </nav>
      </header>
      <%= yield %>
      <footer>
        <p class="text-center">Copyright © Huuste</p>
      </footer>
    </div>
  </body>
</html>
